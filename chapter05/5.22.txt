/*
 * Exercise 5.22: The last example in this section that jumped back to begin
 * could be better written using a loop. Rewrite the code to eliminate the
 * goto.
 *
 * By Faisal Saadatmand
 */

	int sz;
	do {
		sz = get_size();
	} while (sz <= 0);
