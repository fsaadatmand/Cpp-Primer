Exercise 13.56: What would happen if we defined sorted as:

	Foo Foo::sorted() const & {
		Foo ret(*this);
		return ret.sorted();
	}

By Faisal Saadatmand

ret.sorted will cauase a recursive call to sorted.
