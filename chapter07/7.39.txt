Exercise 7.39: Would it be legal for both the constructor that takes a string
and the one that takes an istream& to have default arguments? If not, why not?

By Faisal Saadatmand

No, because the call to the overloaded default constructor would be ambiguous.
